import { render } from "@testing-library/react";
import withAuth from "../../hocs/withAuth";

describe("withAuth HOC tests", () => {
    it("should render children when authenticated", () => {});

    it("should redirect when unauthenticated", () => {});
});
