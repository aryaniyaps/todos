[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.0"
flask-sqlalchemy = ">=2.5"
flask-migrate = ">=3.1"
flask-cors = ">=3.0"
flask-caching = ">=1.10"
passlib = {extras = ["argon2"], version = ">=1.7"}
environs = ">=9.3"
gunicorn = ">=20.1"
redis = ">=3.5"

[dev-packages]

[requires]
python_version = "3.9"
