[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = ">=0.70"
starlette = ">=0.16"
uvicorn = {version = ">=0.15", extras = ["standard"]}
celery = {version = ">=5.1", extras = ["rabbitmq", "redis"]}
sqlalchemy = {version = ">=1.4", extras = ["postgresql_asyncpg"]}
passlib = {version = ">=1.7", extras = ["argon2"]}
elasticsearch = ">=7.15"
elasticsearch-dsl = ">=7.4"
alembic = ">=1.7"
jinja2 = ">=3.0"
pydantic = ">=1.8"

[dev-packages]
pytest = ">=6.2"
pytest-asyncio = ">=0.16"
httpx = ">=0.20"

[requires]
python_version = "3.9"
