[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.0"
flask-mail = ">=0.9"
flask-httpauth = ">=4.5"
flask-cors = ">=3.0"
flask-sqlalchemy = ">=2.5"
flask-migrate = ">=3.1"
psycopg2-binary = ">=2.9"
marshmallow = ">=3.14"
passlib = {version = ">=1.7", extras = ["argon2"]}
gunicorn = ">=20.1"

[dev-packages]
pytest = ">=6.2"
factory-boy = ">=3.2"

[requires]
python_version = "3.9"
