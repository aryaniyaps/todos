[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.0"
flask-sqlalchemy = ">=2.5"
flask-migrate = ">=3.1"
flask-cors = ">=3.0"
flask-caching = ">=1.10"
celery = {version = ">=5.1", extras = ["rabbitmq", "redis"]}
sqlalchemy = {version = ">=1.4", extras = ["postgresql_psycopg2binary"]}
passlib = {version = ">=1.7", extras = ["argon2"]}
redis = ">=3.5"
elasticsearch-dsl = ">=7.4"
gunicorn = ">=20.1"
environs = ">=9.3"

[dev-packages]

[requires]
python_version = "3.9"
