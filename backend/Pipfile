[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = ">=2.0"
alembic = ">=1.7.5"
sqlalchemy = {version=">=1.4.28", extras=["mysql"]}
celery = ">=5.2.3"
passlib = {version=">=1.7", extras=["argon2"]}
marshmallow = ">=3.14"
prometheus-client = ">=0.12"
gunicorn = ">=20.1"

[dev-packages]
pytest = ">=6.2"
factory-boy = ">=3.2"
requests = ">=2.26"

[requires]
python_version = "3.9"
