[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
sanic = ">=21.9"
alembic = ">=1.7"
sqlalchemy = {version=">=1.4", extras=["mysql"]}
celery = {version=">=5.2", extras = ["redis"]}
passlib = {version = ">=1.7", extras = ["argon2"]}
marshmallow = ">=3.14"
uvicorn = ">=0.15"

[dev-packages]
pytest = ">=6.2"
factory-boy = ">=3.2"

[requires]
python_version = "3.9"
