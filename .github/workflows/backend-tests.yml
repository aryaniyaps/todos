name: backend:tests

on:
  push:
    branches:
      - main
    paths:
      - "backend/**"
  pull_request:
    branches:
      - main
    paths:
      - "backend/**"

jobs:
  tests:
    runs-on: ubuntu-20.04
    defaults:
      run:
        working-directory: ./backend
    env:
      FLASK_TESTING: true
      MAIL_SUPPRESS_SEND: true
      SECRET_KEY: "secret"
      SQLALCHEMY_DATABASE_URI: "mysql+pymysql://todos:todos@mysql:3306/todos"

    services:
      mysql:
        image: mysql:8
        ports:
          - "3306:3306"
        env:
          MYSQL_USER: "todos"
          MYSQL_PASSWORD: "todos"
          MYSQL_DATABASE: "todos"

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2

      - name: Setup Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install Pipenv
        run: pip install --no-cache pipenv

      - name: Install Dependencies
        run: pipenv install --dev

      - name: Run Tests
        run: ./scripts/run-tests.sh

      - name: Upload Coverage
        uses: codecov/codecov-action@v2
        with:
          fail_ci_if_error: true
          verbose: true
